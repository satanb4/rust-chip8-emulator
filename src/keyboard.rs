pub struct Keyboard {

}

impl Keyboard {
    pub fn new() -> Keyboard {
        Keyboard{}
    }
    // TODO Implement entire key handling
    pub fn key_pressed(key_code: u8) -> bool{
        true
    }
}